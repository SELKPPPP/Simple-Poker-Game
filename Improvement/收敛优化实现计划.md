# CFR 收敛优化 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重写 info-set 抽象（4.36M → ~35K）并加入 Linear CFR 加权，让 MCCFR 在 PokerGame 上真正收敛。

**Architecture:** 三处代码改动 + 一套验证流程。(1) `mccfr.py` 的 Outcome Sampling 路径加 `weight` 参数实现 Linear CFR；(2) `trainer.py` 每迭代传 `weight=it+1`；(3) `info_set.py` 把手牌折叠成 `(hand_rank, draw_type)`、对手明牌分桶成 `(num_aligned, max_rank//4)`、对手预算截 `min(opp_rem,4)`、自己预算保持精确。然后短训验闸 → 全量重训 → 评测。

**Tech Stack:** Python 3, pytest, 现有 `cfr/` MCCFR 代码库。

> **提交说明**：各任务末尾的 `git commit` 步骤遵循项目惯例——**仅在用户许可后执行**。执行计划时若用户未授权提交，完成代码与测试即可，提交留待用户确认。

---

## 文件结构

| 文件 | 职责 | 改动 |
|---|---|---|
| `cfr/agent/mccfr.py` | OS-MCCFR 算法 | `outcome_sampling`/`_os_episode` 加 `weight`，乘进 regret/cum-strat 增量 |
| `cfr/train/trainer.py` | 训练循环 | 调用处传 `weight=it+1` |
| `cfr/agent/info_set.py` | info-set 编码 | 重写 `encode_info_set`，加 `_opp_face_up_bucket`，删 `_opp_face_up_features` |
| `cfr/tests/test_info_set.py` | 编码单测 | 更新失效断言，加新桶/合法动作一致性测试 |
| `cfr/tests/test_mccfr_linear.py` | 新增 | Linear CFR 加权单测 |
| `cfr/tests/test_trainer.py` | trainer 单测 | 加 `weight=it+1` 传递测试 |
| `Improvement/收敛优化设计.md` | 设计 spec | 已写 |
| `Improvement/CHANGELOG.md`、`项目知识库.md` | 文档 | 收尾更新 |

---

## Task 1: Linear CFR 加权（mccfr.py OS 路径）

**Files:**
- Create: `cfr/tests/test_mccfr_linear.py`
- Modify: `cfr/agent/mccfr.py:69-82`（`outcome_sampling` 签名）、`cfr/agent/mccfr.py:85-147`（`_os_episode`）

- [ ] **Step 1: 写失败测试**

`cfr/tests/test_mccfr_linear.py`：

```python
"""Linear CFR weighting on the Outcome Sampling path.

With identical rng seed + game, the sampled trajectory and every a* choice are
identical across runs (both start from empty tables → uniform sigma everywhere).
So the ONLY difference a weight W makes is a linear scale on every regret and
cumulative-strategy increment. We assert exactly that.
"""

import random

from cfr.agent.info_set import encode_info_set
from cfr.agent.mccfr import outcome_sampling
from cfr.agent.regret_table import RegretTable
from cfr.env.game import PokerGame


def _fn(g, p):
    return encode_info_set(g.observation(p))


def test_weight_scales_cumulative_strategy_and_regret_linearly():
    t1 = RegretTable()
    outcome_sampling(PokerGame(seed=7), 0, t1, _fn, random.Random(123), weight=1.0)

    t10 = RegretTable()
    outcome_sampling(PokerGame(seed=7), 0, t10, _fn, random.Random(123), weight=10.0)

    # identical trajectory → identical info-set keys touched
    assert set(t1.info_sets()) == set(t10.info_sets())
    assert len(t1.info_sets()) > 0

    for iset in t1.info_sets():
        cs1 = t1._cumulative_strategy[iset]
        cs10 = t10._cumulative_strategy[iset]
        for a in cs1:
            assert abs(cs10[a] - 10.0 * cs1[a]) <= 1e-6 * max(1.0, abs(cs10[a]))
        r1 = t1._regrets[iset]
        r10 = t10._regrets[iset]
        for a in r1:
            assert abs(r10[a] - 10.0 * r1[a]) <= 1e-6 * max(1.0, abs(r10[a]))


def test_weight_defaults_to_one():
    # default weight (omitted) must equal explicit weight=1.0
    t_def = RegretTable()
    outcome_sampling(PokerGame(seed=11), 0, t_def, _fn, random.Random(99))
    t_one = RegretTable()
    outcome_sampling(PokerGame(seed=11), 0, t_one, _fn, random.Random(99), weight=1.0)
    for iset in t_def.info_sets():
        assert t_def._cumulative_strategy[iset] == t_one._cumulative_strategy[iset]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest cfr/tests/test_mccfr_linear.py -v`
Expected: FAIL — `outcome_sampling() got an unexpected keyword argument 'weight'`

- [ ] **Step 3: 实现 —— 给 `outcome_sampling` 和 `_os_episode` 加 `weight`**

改 `cfr/agent/mccfr.py` 的 `outcome_sampling`（当前 69-82 行）：

```python
def outcome_sampling(
    game,
    traverser: int,
    regret_table: RegretTable,
    info_set_fn: Callable,
    rng: random.Random,
    epsilon: float = 0.6,
    weight: float = 1.0,
) -> float:
    """One OS-MCCFR episode from current game. Returns value_estimate at root
    (not directly meaningful at call site).

    weight: Linear CFR iteration weight (typically t = iteration index). Scales
    both regret and cumulative-strategy increments so late, regret-differentiated
    visits outweigh early IS-inflated uniform ones.
    """
    return _os_episode(
        game, traverser, regret_table, info_set_fn, rng,
        my_reach=1.0, opp_reach=1.0, sample_reach=1.0, epsilon=epsilon,
        weight=weight,
    )
```

改 `_os_episode` 签名（当前 85-89 行），末尾加 `weight: float`：

```python
def _os_episode(
    game, traverser: int, regret_table: RegretTable, info_set_fn: Callable,
    rng: random.Random, my_reach: float, opp_reach: float, sample_reach: float,
    epsilon: float, weight: float,
) -> float:
```

把递归调用（当前 125-129 行）补上 `weight=weight`：

```python
    child_value = _os_episode(
        child, traverser, regret_table, info_set_fn, rng,
        my_reach=new_my, opp_reach=new_opp,
        sample_reach=sample_reach * p_sample, epsilon=epsilon,
        weight=weight,
    )
```

把 traverser 更新块（当前 138-145 行）乘上 `weight`：

```python
    if current == traverser:
        cf_value = value_estimate * opp_reach / sample_reach
        for a in legal:
            cf_action_value = (sampled_corrected if a == a_star else 0.0) \
                              * opp_reach / sample_reach
            regret_table.update_regret(info_set, a, weight * (cf_action_value - cf_value))
        increment = {a: weight * my_reach * sigma[a] / sample_reach for a in legal}
        regret_table.update_cumulative_strategy(info_set, increment)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest cfr/tests/test_mccfr_linear.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 确认未破坏 Kuhn（external_sampling 未动）**

Run: `python -m pytest cfr/tests/test_kuhn.py -v`
Expected: PASS（全绿）

- [ ] **Step 6: 提交**（经用户许可后）

```bash
git add cfr/agent/mccfr.py cfr/tests/test_mccfr_linear.py
git commit -m "feat(cfr): add Linear CFR weighting to outcome sampling"
```

---

## Task 2: trainer 传 `weight=it+1`

**Files:**
- Modify: `cfr/train/trainer.py:68`
- Test: `cfr/tests/test_trainer.py`

- [ ] **Step 1: 写失败测试**（追加到 `cfr/tests/test_trainer.py` 末尾）

```python
def test_trainer_passes_linear_weight(monkeypatch):
    """trainer must call outcome_sampling with weight = it+1 each iteration."""
    import cfr.train.trainer as tr

    captured = []

    def fake_os(game, traverser, table, fn, rng, weight=1.0):
        captured.append(weight)

    monkeypatch.setattr(tr, "outcome_sampling", fake_os)
    with tempfile.TemporaryDirectory() as d:
        tr.train(_minimal_config(5, d))
    assert captured == [1, 2, 3, 4, 5]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest cfr/tests/test_trainer.py::test_trainer_passes_linear_weight -v`
Expected: FAIL — `captured == [None,...]` 或 AssertionError（当前未传 weight）

- [ ] **Step 3: 实现 —— 改 trainer 调用处**

`cfr/train/trainer.py:68`，把：

```python
        outcome_sampling(game, traverser, table, _info_set_fn, rng)
```

改成：

```python
        outcome_sampling(game, traverser, table, _info_set_fn, rng, weight=it + 1)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest cfr/tests/test_trainer.py -v`
Expected: PASS（含原有 smoke 测试全绿）

- [ ] **Step 5: 提交**（经用户许可后）

```bash
git add cfr/train/trainer.py cfr/tests/test_trainer.py
git commit -m "feat(cfr): pass Linear CFR weight=t from trainer"
```

---

## Task 3: 重写 info_set 编码（4.36M → ~35K）

**Files:**
- Modify: `cfr/agent/info_set.py:31-56`（`encode_info_set`）；删 `_opp_face_up_features`（116-130 行）；加 `_opp_face_up_bucket`
- Test: `cfr/tests/test_info_set.py`（Task 4 处理失效断言）

- [ ] **Step 1: 写新行为测试**（追加到 `cfr/tests/test_info_set.py` 末尾）

```python
# ============ v4 coarse abstraction (convergence redesign) ============

from cfr.agent.info_set import _opp_face_up_bucket


def test_opp_bucket_counts_aligned_and_max_rank():
    # my dominant suit = Spades(0). opp face-up: 2 spades + 1 club, max rank K(=12).
    fu = (None, None, card(2, SUIT_S), card(7, SUIT_S), card("K", SUIT_C))
    assert _opp_face_up_bucket(fu, my_dominant_suit=SUIT_S) == (2, 12 // 4)


def test_opp_bucket_no_visible_cards():
    assert _opp_face_up_bucket((None, None, None, None, None), my_dominant_suit=0) == (0, -1)


def test_top_card_no_longer_splits_keys():
    # Two HighCard hands, same hand_rank + same draw_type, different top card.
    # v4 drops top_card → SAME key.
    h_kicker_k = [card(2, SUIT_S), card(5, SUIT_H), card(7, SUIT_D),
                  card(9, SUIT_C), card("K", SUIT_S)]
    h_kicker_q = [card(2, SUIT_S), card(5, SUIT_H), card(7, SUIT_D),
                  card(9, SUIT_C), card("Q", SUIT_S)]
    assert encode_info_set(make_obs(h_kicker_k)) == encode_info_set(make_obs(h_kicker_q))


def test_my_remaining_redraws_kept_exact():
    # legal-action consistency: my budget 7 vs 4 must stay distinct keys.
    h = [card(2, SUIT_S), card(5, SUIT_H), card(7, SUIT_D),
         card(9, SUIT_C), card("K", SUIT_S)]
    assert encode_info_set(make_obs(h, my_remaining_redraws=7)) != \
        encode_info_set(make_obs(h, my_remaining_redraws=4))


def test_opp_remaining_redraws_capped_at_4():
    # opp budget does NOT affect my legal actions → capped: 5,6,7 collapse.
    h = [card(2, SUIT_S), card(5, SUIT_H), card(7, SUIT_D),
         card(9, SUIT_C), card("K", SUIT_S)]
    assert encode_info_set(make_obs(h, opp_remaining_redraws=7)) == \
        encode_info_set(make_obs(h, opp_remaining_redraws=5))
    # but below the cap still splits:
    assert encode_info_set(make_obs(h, opp_remaining_redraws=7)) != \
        encode_info_set(make_obs(h, opp_remaining_redraws=3))


def test_redraw_history_does_not_split_keys():
    # v4 drops this-round redraw detail → these now collapse.
    h = [card(2, SUIT_S), card(5, SUIT_H), card(7, SUIT_D),
         card(9, SUIT_C), card("K", SUIT_S)]
    assert encode_info_set(make_obs(h, my_redraw_actions_this_round=())) == \
        encode_info_set(make_obs(h, my_redraw_actions_this_round=(16,)))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest cfr/tests/test_info_set.py -k "v4 or opp_bucket or top_card or remaining_redraws_kept or capped or redraw_history_does_not" -v`
Expected: FAIL —— `ImportError: cannot import name '_opp_face_up_bucket'`

- [ ] **Step 3: 实现 —— 加 `_opp_face_up_bucket`，重写 `encode_info_set`，删 `_opp_face_up_features`**

在 `cfr/agent/info_set.py` 加新 helper（放在 `_opp_face_up_features` 原位置，替换它）：

```python
def _opp_face_up_bucket(
    opp_visible_cards: Tuple[Optional[int], ...],
    my_dominant_suit: int,
) -> Tuple[int, int]:
    """Coarse bucket of opp face-up cards: (num_aligned, max_rank // 4).

    num_aligned = count of visible cards sharing my dominant suit (flush-chase
    signal). max_rank bucketed to 4 bands; -1 when nothing visible.
    """
    num_aligned = 0
    max_rank = -1
    for c in opp_visible_cards:
        if c is None:
            continue
        rank, suit = get_rank_suit(c)
        if my_dominant_suit != -1 and suit == my_dominant_suit:
            num_aligned += 1
        if rank > max_rank:
            max_rank = rank
    rank_bucket = max_rank // 4 if max_rank >= 0 else -1
    return (num_aligned, rank_bucket)
```

把 `encode_info_set`（当前 31-56 行）整体替换为：

```python
def encode_info_set(obs: Observation) -> str:
    """v4 coarse abstraction (~35K reachable info sets).

    Collapses hand to (hand_rank, draw_type), buckets opp face-up to
    (num_aligned, max_rank//4), caps opp budget at 4. my_remaining_redraws
    stays exact for legal-action consistency. See Improvement/收敛优化设计.md.
    """
    hand_rank, _ = _hand_rank_and_top(obs.my_hand)
    dominant_suit, _ = _suited_mask(obs.my_hand)
    _, draw_type = _straight_features(obs.my_hand)
    opp_bucket = _opp_face_up_bucket(obs.opp_visible_cards, dominant_suit)

    key = (
        hand_rank, draw_type,
        opp_bucket,
        obs.my_remaining_redraws,            # exact (legal-action consistency)
        min(obs.opp_remaining_redraws, 4),   # capped (opp budget ∉ my legal set)
        obs.my_round_wins, obs.opp_round_wins,
        obs.phase, obs.round_num,
    )
    return repr(key)
```

注：`_hand_rank_and_top`、`_suited_mask`、`_straight_features` 保留——分别提供 `hand_rank`、`dominant_suit`（喂给 opp 桶）、`draw_type`。删除旧的 `_opp_face_up_features`（已被 `_opp_face_up_bucket` 取代）。

- [ ] **Step 4: 跑新测试确认通过**

Run: `python -m pytest cfr/tests/test_info_set.py -k "opp_bucket or top_card or remaining_redraws_kept or capped or redraw_history_does_not" -v`
Expected: PASS

- [ ] **Step 5: 不提交，进入 Task 4**（旧测试此时还会红，先修完再一起提交）

---

## Task 4: 更新 test_info_set.py 中失效的旧断言

**Files:**
- Modify: `cfr/tests/test_info_set.py`

旧编码的几个测试现在语义变了，需更新。**只改下列具体项**，其余（draw_type/suited_mask 的 helper 测试、determinism、canonical、phase、round_wins、opp_face_up alignment/order、real_game smoke）保持不动——它们仍然成立。

- [ ] **Step 1: 跑全文件，列出失败项**

Run: `python -m pytest cfr/tests/test_info_set.py -v`
Expected: 这些 FAIL：
- `test_different_redraw_history_different_keys`（断言不等，现在相等了）
- `test_opp_face_up_features_handles_none`（`_opp_face_up_features` 已删除 → ImportError）

> 若还有其它失败项，逐一按"语义是否被 v4 有意改变"判断：被有意合并的改断言，未受影响却失败的说明实现有 bug，回 Task 3 查。

- [ ] **Step 2: 删除/改写失效测试**

删掉 `test_different_redraw_history_different_keys`（256-260 行附近，整函数删除）——其行为已被 Task 3 的 `test_redraw_history_does_not_split_keys` 取代（断言相等）。

删掉 `test_opp_face_up_features_handles_none`（286-289 行附近，整函数删除）——`_opp_face_up_features` 已移除，等价覆盖由 Task 3 的 `test_opp_bucket_no_visible_cards` 提供。

同时删掉文件顶部 import 里的 `_opp_face_up_features`（第 15 行附近），保留 `_straight_features`、`_suited_mask`：

```python
from cfr.agent.info_set import (
    DRAW_GUTSHOT,
    DRAW_NONE,
    DRAW_OPEN_ENDED,
    DRAW_STRAIGHT_MADE,
    _straight_features,
    _suited_mask,
    encode_info_set,
)
```

- [ ] **Step 3: 跑全文件确认全绿**

Run: `python -m pytest cfr/tests/test_info_set.py -v`
Expected: PASS（全部）

> 若 `test_two_games_different_seeds_different_keys`（seed 1 vs 2）因粗抽象产生碰撞而失败：把它改用 `seed=1` vs `seed=3`（或任一对编码出不同 key 的种子），保持"不同牌局→不同 key"的意图。先按原样跑，红了再改。

- [ ] **Step 4: 跑全套测试确认无连带破坏**

Run: `python -m pytest cfr/tests/ -q`
Expected: 全绿（test_cfr_agent / test_play / test_sampled_br 等依赖 encode_info_set 的也应通过——它们只要求返回合法 str key）

- [ ] **Step 5: 提交 Task 3+4**（经用户许可后）

```bash
git add cfr/agent/info_set.py cfr/tests/test_info_set.py
git commit -m "feat(cfr): coarse v4 info-set abstraction (4.36M -> ~35K)"
```

---

## Task 5: 验证脚本（info-set 计数 + uniform% 检查器）

**Files:**
- Create: `cfr/scripts/check_convergence.py`

- [ ] **Step 1: 写脚本**

```python
"""Convergence diagnostics for a checkpoint (or a freshly-trained table).

Usage:
    python -m cfr.scripts.check_convergence --checkpoint cfr/checkpoints/<f>.pkl

Reports:
  - info_set_count
  - uniform cum_strat %  (target < 30%; was 93.1% in the failed 1M run)
"""

import argparse

from cfr.train.checkpoint import load_checkpoint


def uniform_fraction(table) -> tuple:
    uniform = total = 0
    for cs in table._cumulative_strategy.values():
        if len(cs) <= 1:
            continue
        total += 1
        vs = list(cs.values())
        if max(vs) - min(vs) < 1e-3:
            uniform += 1
    return uniform, total


def report(table) -> None:
    n = len(table.info_sets())
    u, t = uniform_fraction(table)
    pct = (100.0 * u / t) if t else 0.0
    print(f"info_set_count      : {n:,}")
    print(f"multi-action isets  : {t:,}")
    print(f"uniform cum_strat   : {u:,}/{t:,} = {pct:.1f}%   (target < 30%)")


def main() -> None:
    p = argparse.ArgumentParser()
    p.add_argument("--checkpoint", required=True)
    args = p.parse_args()
    table = load_checkpoint(args.checkpoint)["regret_table"]
    report(table)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 冒烟跑（用旧失败 checkpoint，确认脚本正确复现 93%）**

Run: `python -m cfr.scripts.check_convergence --checkpoint cfr/checkpoints/cfr_a0c986b_1000000.pkl`
Expected: `uniform cum_strat ... = 93.1%`（与诊断 §3 一致 → 脚本可信）

- [ ] **Step 3: 提交**（经用户许可后）

```bash
git add cfr/scripts/check_convergence.py
git commit -m "feat(cfr): add convergence diagnostic script"
```

---

## Task 6: 短训验闸（50K iter）

> 这是 go/no-go 闸门。通过才投入全量重训。

- [ ] **Step 1: 短训 50K iter**

Run: `python -m cfr.scripts.train --num-iter 50000`
Expected: 正常跑完，落一个 `cfr/checkpoints/cfr_<hash>_50000.pkl`。记录 info_sets 数（应在 ~万级，远小于 4.36M）。

- [ ] **Step 2: 检查 uniform%**

Run: `python -m cfr.scripts.check_convergence --checkpoint cfr/checkpoints/cfr_<hash>_50000.pkl`
**闸门 1**：`uniform cum_strat % < 30%`。

- [ ] **Step 3: 看 exploit 趋势**

查看 `cfr/logs/train_<hash>.csv` 的 `sampled_exploit` 列（默认 `eval_every=10000`，会有 5 个点）。
**闸门 2**：相比旧 run 的 1.69–1.86 平台，应出现下降趋势。

- [ ] **Step 4: 判定**

- 两个闸门都过 → 进 Task 7（全量重训）。
- 闸门 1 没过（uniform% 仍高）→ 先降 `epsilon`（在 `cfr/agent/mccfr.py` 的 `outcome_sampling` 默认 0.6 → 试 0.3），重跑 Step 1；仍不行则把抽象改到更粗的 T3（opp 桶仅留 `num_aligned`，见设计 §3.2），重跑。
- 不要在没过闸门时投全量训练。

---

## Task 7: 全量重训（1M iter）

- [ ] **Step 1: 启动 1M 训练**（约 4h，建议后台跑）

Run: `python -m cfr.scripts.train --num-iter 1000000`
Expected: 周期性 checkpoint + log；最终 `cfr/checkpoints/cfr_<hash>_1000000.pkl`。

- [ ] **Step 2: 终态 uniform% 复查**

Run: `python -m cfr.scripts.check_convergence --checkpoint cfr/checkpoints/cfr_<hash>_1000000.pkl`
Expected: `uniform cum_strat % < 30%`（确认全量后仍收敛，未回退）。

---

## Task 8: 评测 vs 5 baseline + 文档收尾

- [ ] **Step 1: 跑评测脚本**（诊断 §8 的脚本，换成新 checkpoint 路径）

```python
from cfr.eval.cfr_agent import CFRAgent
from cfr.eval.play import play_match
from cfr.eval.rule_agents import (
    RandomAgent, AlwaysCallAgent, TightAgent, LooseAgent, HeuristicAgent
)
cfr = CFRAgent.from_checkpoint("cfr/checkpoints/cfr_<hash>_1000000.pkl")
for name, opp in [
    ("random",      RandomAgent(seed=1)),
    ("always_call", AlwaysCallAgent()),
    ("tight",       TightAgent()),
    ("loose",       LooseAgent()),
    ("heuristic",   HeuristicAgent()),
]:
    r = play_match(cfr, opp, num_games=500, seed=42)
    print(f"{name:<14} a_avg_u={r['a_avg_utility']:+.3f}  "
          f"W-T-L: {r['a_wins']}-{r['ties']}-{r['b_wins']}")
```

**成功标准**：对 `always_call` 由 −0.64 转正（P(fold)→0）；整体多数 baseline 较旧 run 改善。

- [ ] **Step 2: 全套测试回归**

Run: `python -m pytest cfr/tests/ -q`
Expected: 全绿。

- [ ] **Step 3: 更新文档**

- `Improvement/CHANGELOG.md`：加一条 v4 抽象 + Linear CFR + 重训结果。
- `Improvement/项目知识库.md`：更新 §4.5.6 / §6.3 当前位置；修正诊断 §6.1 的过期判断（删历史无效，真正解法是分桶）——指向 `收敛优化设计.md` §2。
- `Improvement/训练未收敛诊断.md`：在 §6.1 顶部加一行勘误，链到 `收敛优化设计.md`。

- [ ] **Step 4: 提交**（经用户许可后）

```bash
git add Improvement/ cfr/
git commit -m "docs(cfr): record v4 convergence results"
```

---

## Self-Review 覆盖核对

| 设计 spec 节 | 对应任务 |
|---|---|
| §3.2 info_set 重写（hand 折叠 / opp 桶 / opp_rem 截断 / my_rem 精确） | Task 3 |
| §3.3 Linear CFR（t 加权） | Task 1 + Task 2 |
| §3.4 合法动作一致性（my_rem 精确，opp_rem 可截） | Task 3（`test_my_remaining_redraws_kept_exact` / `test_opp_remaining_redraws_capped_at_4`） |
| §3.1 成功标准（uniform%<30、exploit↓、vs always_call 转正） | Task 6（闸门）+ Task 8（评测） |
| §3.5 验证流程（短训→闸门→全量→评测） | Task 6 → 7 → 8 |
| §6 投影工具复核 | Task 5（check_convergence）+ 已在规划阶段算出 35,331 |
